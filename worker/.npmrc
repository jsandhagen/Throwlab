# npm 10's resolver crashes on vitest 4's optional peer graph
# ("Cannot read properties of null (reading 'edgesOut')" out of arborist).
# Nothing here actually needs strict peer resolution, and this is what makes
# `npm install` work on the npm most people have.
legacy-peer-deps=true
